[package]
name = "cgio_build"
version.workspace = true
edition.workspace = true
description = "Shared build-script helper for C++ bot crates: compile main.cpp into a static archive that the crate's `[[bin]]` links against."

[dependencies]
cc.workspace = true
