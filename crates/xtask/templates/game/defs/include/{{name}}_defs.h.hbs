// Hand-written C++ mirror of {{name}}_defs/src/lib.rs. Both sides are
// independent — when you change the wire types here, change them
// there too.
//
// SKELETON: add your game's type definitions (enums, structs) below.
// See `tron/defs/include/tron_defs.h` for a worked example.

#pragma once

#include <cstdint>

// TODO: TurnOutput's fields.
struct TurnOutput {};
