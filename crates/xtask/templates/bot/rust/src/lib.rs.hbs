use {{game}}_defs::{InitialInput, TurnInput, TurnOutput};

#[derive(Default)]
pub struct GameState {}

// Match-start hook. For games without per-player init, `init` is the
// empty `()` struct and this is a no-op;
pub fn on_init(_init: &InitialInput, _state: &mut GameState) {}

// TODO: replace with your strategy.
pub fn decide(turn: &TurnInput, _state: &mut GameState) -> TurnOutput {
    TurnOutput::default()
}
