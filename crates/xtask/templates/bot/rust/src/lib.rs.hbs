use {{game}}_defs::{InitialInput, TurnInput, TurnOutput};

// Match-start hook. For games without per-player init, `init` is the
// empty `()` struct and this is a no-op; stash whatever you need
// from `init` into a `static OnceLock` here if the game ships init
// data.
pub fn on_init(_init: &InitialInput) {}

// TODO: replace with your strategy.
pub fn decide(_turn: &TurnInput) -> TurnOutput {
    TurnOutput::default()
}
