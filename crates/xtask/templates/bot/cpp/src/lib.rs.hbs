//! C++ bot for {{game}} ({{bot}} variant) — see `bot.cpp` at the crate root.
//!
//! The Rust surface is intentionally empty: this crate exists so
//! `cargo build -p {{crate_name}}` produces a `cdylib` (loaded by
//! the runner) with `bot.cpp` compiled and force-loaded in through
//! `cgio_build` + `build.rs`.
