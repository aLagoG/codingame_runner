// {{game}} bot strategy — {{bot}}.
//
// Single source of truth for the per-turn logic. `main.cpp` includes
// this header and calls `on_init(init)` once + `decide(turn)` per tick.
// Edit the strategy here; both local cargo builds and the next
// `cargo xtask bundle` for CodinGame pick it up.

#pragma once

#include "../../defs/include/{{game}}_defs_io.h"

namespace {{crate_name}} {

// Match-start hook. For games without per-player init, `init` is an
// empty `cgio::InitialInput` — ignore it. Otherwise, stash what you
// need from `init` into a static at the top of this file.
inline void on_init(const cgio::InitialInput& /*init*/) {}

// Per-turn entry point. Replace the body with the bot's logic.
inline TurnOutput decide(const cgio::TurnInput& /*turn*/) {
    return TurnOutput{};
}

}  // namespace {{crate_name}}
