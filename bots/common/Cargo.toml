[package]
name = "bot_common"
version.workspace = true
edition.workspace = true
description = "Bot-facing trait + macro surface shared across every CodinGame bot. Deliberately std-only — no third-party deps — so flattened bots stay vendor-clean for CodinGame submission."

[dependencies]
